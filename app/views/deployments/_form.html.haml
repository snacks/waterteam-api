= form_with(model: deployment, local: true) do |form|
  - if deployment.errors.any?
    #error_explanation
      %h2
        = pluralize(deployment.errors.count, "error")
        prohibited this deployment from being saved:
      %ul
        - deployment.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name, id: :deployment_string
  .actions
    = form.submit
