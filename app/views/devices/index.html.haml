%p#notice= notice
%h1 Devices
%table
  %thead
    %tr
      %th Uniqueidentifier
      %th Type
      %th Latitude
      %th Longitude
      %th{:colspan => "3"}
  %tbody
    - @devices.each do |device|
      %tr
        %td= device.uniqueIdentifier
        %td= device.type
        %td= device.latitude
        %td= device.longitude
        %td= link_to 'Show', device
        %td= link_to 'Edit', edit_device_path(device)
        %td= link_to 'Destroy', device, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Device', new_device_path
