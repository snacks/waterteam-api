%p#notice= notice
%h1 Uploads
= link_to 'DestroyALL', uploads_path, method: :delete, data: { confirm: 'Are you sure?' }
%table
  %thead
    %tr
      %th Filename
      %th Processed
      %th{:colspan => "3"}
  %tbody
    - @uploads.each do |upload|
      %tr
        %td= upload.filename
        %td= upload.processed
        %td= link_to 'Show', upload
        %td= link_to 'Edit', edit_upload_path(upload)
        %td= link_to 'Destroy', upload, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Upload', new_upload_path
